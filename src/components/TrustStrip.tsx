import type { ClientConfig } from "@/lib/types";

/** Trust badges live in the cream hero; keep export for compatibility. */
export function TrustStrip(_props: { client: ClientConfig }) {
  return null;
}
